"use strict";

var _constants = require("../constants");

module.exports = {
  native_module: {
    require: require,
    wrap: function wrap(src) {
      return "(function (exports, require, module, __filename, __dirname) { \n" + src + "\n});";
    },
    nonInternalExists: function nonInternalExists(id) {
      return _constants.nonInternalModules.filter(function (_id) {
        return id === _id;
      }).length > 0;
    },
    exists: function exists() {
      return false;
    }
  }
};